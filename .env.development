# Local dev default: points at a call API running locally (see
# game-master-bell-api's own dev instructions once that repo has one).
# Override with a gitignored `.env.local` if your local API runs elsewhere.
VITE_API_URL=http://127.0.0.1:8787
